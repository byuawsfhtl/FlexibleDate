name: Deploy

on:
  push:
    branches: [prd, dev]
    paths-ignore: # Pushes that include only these changed files won't trigger actions
      - "**/README.md"
      - "**/.gitignore"
      - "**/docs/*"
      - "**/.github/*"
      - "**/tests/*"
      - "**/_version.py"

permissions:
  id-token: write
  contents: read

jobs:
  update-version:
    name: Update Version
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: Update Version
        uses: byuawsfhtl/UpdateVersion@v1.0.9
        with:
          token: ${{ secrets.RLL_BOT_PA_TOKEN }}
          versionPath: ${{ github.workspace }}/_version.py

  build-python:
    name: Build Python distribution 📦
    if: github.ref == 'refs/heads/prd' # Only publish when pushing to prd
    needs: update-version
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }} # Explicitly checkout the branch that triggered the workflow
          # This ensures we're using the latest commit including version updates
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Install pypa/build
        run: python3 -m pip install build
      - name: Build a binary wheel and a source tarball
        run: python3 -m build
      - name: Store the distribution packages
        uses: actions/upload-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

  publish-to-pypi:
    name: Publish Python 🐍 distribution 📦 to PyPI
    if: github.ref == 'refs/heads/prd' # Only publish when pushing to prd
    needs: build-python
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/FlexibleDate
    permissions:
      id-token: write

    steps:
      - uses: actions/checkout@v4 # Needed to ensure repo context
      - name: Download all the dists
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/
      - name: Verify dist exists
        run: ls -l dist/
      - name: Publish distribution 📦 to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_ACCESS_TOKEN }}

  build-typescript:
    name: Build TypeScript distribution 📦
    if: github.ref == 'refs/heads/prd' # Only publish when pushing to prd
    needs: update-version
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          registry-url: 'https://registry.npmjs.org'
      - name: Sync version from _version.py to package.json
        run: |
          VERSION=$(python3 -c "exec(open('_version.py').read()); print(__version__)")
          cd FlexibleDateTS
          npm version $VERSION --no-git-tag-version --allow-same-version
      - name: Install dependencies
        working-directory: ./FlexibleDateTS
        run: npm ci
      - name: Build TypeScript
        working-directory: ./FlexibleDateTS
        run: npm run build
      - name: Store the TypeScript distribution
        uses: actions/upload-artifact@v4
        with:
          name: typescript-package-distributions
          path: FlexibleDateTS/

  publish-to-npm:
    name: Publish TypeScript 📘 distribution 📦 to npm
    if: github.ref == 'refs/heads/prd' # Only publish when pushing to prd
    needs: build-typescript
    runs-on: ubuntu-latest
    environment:
      name: npm
      url: https://www.npmjs.com/package/flexibledatets

    steps:
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'
          always-auth: true
        
      - name: Ensure npm supports OIDC (upgrade if needed)
        run: npm install -g "npm@^11.5.1"

      - name: Download TypeScript distribution
        uses: actions/download-artifact@v4
        with:
          name: typescript-package-distributions
          path: FlexibleDateTS/

      - name: Verify dist exists
        run: ls -l FlexibleDateTS/dist/

      - name: Publish to npm
        working-directory: ./FlexibleDateTS
        run: NPM_CONFIG_PROVENANCE=false npm publish --access public
